export default function AboutPage() {
  return (
    <main className="min-h-screen w-full px-6 py-10 lg:px-10">
      <section className="rounded-[2rem] border border-black/10 bg-white/80 p-8 shadow-soft">
        <p className="text-sm uppercase tracking-[0.2em] text-smoke">About</p>
        <h1 className="mt-3 text-3xl font-bold text-ink">A practical marketplace rebuild</h1>
        <p className="mt-3 text-sm leading-7 text-smoke">
          This project is being rebuilt around clear approval flows, simpler operations, and a better structure for future automation.
        </p>
      </section>
    </main>
  );
}
